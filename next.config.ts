import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The default, bottom-left, covers the sidebar's Settings link.
  devIndicators: { position: "bottom-right" },
};

export default nextConfig;
